/*
	TypeInfo
	xCore library
	2015 © Ivan Muzyka
	Licensed under the MIT license
*/

#pragma once
#include "xCore/Types.h"
/*
// SByte type static values
class SByte
{
public:
	static const sbyte MinValue = sbyte(0x80);	// Minimum value of type sbyte	
	static const sbyte MaxValue = 0x7F;			// Maximum value of type sbyte	
	static const int Size = sizeof(sbyte);		// Size in memory
private:
	SByte() {};
};

// Byte type static values
class Byte
{
public:
	static const byte MinValue = 0;				// Minimum value of type byte	
	static const byte MaxValue = 0xFF;			// Maximum value of type byte	
	static const uint Size = sizeof(byte);		// Size in memory
private:
	Byte() {};
};


// Short type static values
class Short
{
public:	
	static const short MinValue = short(0x8000);// Minimum value of type short	
	static const short MaxValue = short(0x7FFF);// Maximum value of type short	
	static const int Size = sizeof(short);		// Size in memory
private:
	Short() {};
};

// UShort type static values
class UShort
{
public:	
	static const ushort MinValue = 0;			// Minimum value of type ushort	
	static const ushort MaxValue = 0xFFFF;		// Maximum value of type ushort	
	static const uint Size = sizeof(ushort);	// Size in memory
private:
	UShort() {};
};

// Int type static values
class Int
{
public:	
	static const int MinValue = 0x80000000;	// Minimum value of type int	
	static const int MaxValue = 0x7FFFFFFF;		// Maximum value of type int
	static const uint Size = sizeof(int);		// Size in memory
private:
	Int() {};
};

// Int type static values
class UInt
{
public:	
	static const uint MinValue = 0;				// Minimum value of type uint	
	static const uint MaxValue = 0xFFFFFFFF;	// Maximum value of type uint	
	static const uint Size = sizeof(uint);		// Size in memory
private:
	UInt() {};
};

// Long type static values
class Long
{
public:	
	static const long MinValue = 0x80000000;	// Minimum value of type long	
	static const long MaxValue = 0x7FFFFFFF;	// Maximum value of type long	
	static const uint Size = sizeof(long);		// Size in memory
private:
	Long() {};
};

// ULong type static values
class ULong
{
public:
	static const long MinValue = 0;				// Minimum value of type ulong
	static const long MaxValue = 0xFFFFFFFF;	// Maximum value of type ulong
	static const uint Size = sizeof(ulong);		// Size in memory
private:
	ULong() {};
};*/
